---
title: Overview
description: Skills vs the Agent Plugin, and the deterministic-invocation rules.
---

_Coming soon._ Agent Skills vs the Agent Plugin in this project's terms, the
"binary installed separately" invariant, and the determinism rules (`-o json`,
`send` blocks by default, check `status.state`).
