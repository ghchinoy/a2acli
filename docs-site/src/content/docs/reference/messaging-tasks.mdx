---
title: Messaging & Tasks
description: send, subscribe, get, list tasks, cancel, download, and push-config.
---

_Coming soon._ `send`, `subscribe`, `get`, `list tasks`, `cancel`, `download`,
and `push-config` (plus the `task …` grammar).
