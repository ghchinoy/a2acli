---
title: Configuration & Auth
description: config / config show / config env and auth login | status | token | logout.
---

_Coming soon._ `config` / `config show` / `config env` and
`auth login | status | token | logout` (OAuth 2.1 with auth-code + PKCE).
