---
title: Installation
description: Install a2acli via Homebrew, install.sh, packages, or from source.
---

_Coming soon._ Installation via Homebrew, `install.sh`, apt / rpm / winget,
`go install`, or from source — plus how to verify the install.
